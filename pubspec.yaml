name: hr_demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  get: ^4.6.6
  image_picker: ^1.0.7
  mysql1: ^0.20.0
  cached_network_image: ^3.3.1
  pull_to_refresh: ^2.0.0
  shared_preferences: ^2.5.4
  dio: ^5.9.0
  requests: ^4.7.0
  pinput: ^6.0.1
  camera: ^0.10.5+5
  local_auth: ^2.1.0
  image: ^4.1.7
  connectivity_plus: ^5.0.0
  google_mlkit_face_detection: ^0.11.0
  # no external settings packages added to avoid build issues

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  

flutter:
  uses-material-design: true
  assets:
  - assets/images/


